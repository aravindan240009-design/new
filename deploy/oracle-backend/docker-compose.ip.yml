services:
  hostel-backend:
    build:
      context: ../../backend
      dockerfile: Dockerfile
    container_name: hostel-backend
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "8080:8080"
